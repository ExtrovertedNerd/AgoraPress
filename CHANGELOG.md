# Changelog

All notable changes to AgoraPress will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

Core currently reports `AP_VERSION` `0.1.0-dev` (see `ap-includes/version.php`).
No tagged public release yet — ship notes accumulate under **[Unreleased]** until the first version is cut.

## [Unreleased]

### Added

- **One-click core auto-update:** `AP_Core_Updater` downloads the package URL from the public `version.json` payload (GET only; optional SHA-256 via `sha256` / `package_sha256`), validates zip safety (path traversal / size soft limits), extracts to a staging tree, requires a recognizable AgoraPress root (`index.php` + `ap-includes/version.php` + `ap-admin/`), applies core files while **preserving** `ap-config.php`, `ap-content/uploads/`, plugins, mu-plugins, and custom themes (default `agora` theme is updated from the package), enables `.maintenance` for front-end visitors during apply, runs pending `AP_Migrator` schema migrations, clears the version-check transient, and records `ap_last_core_update`. Admin **Tools → Update Core** (`ap-admin/update-core.php`, cap `update_core`): re-check, one-click update with nonce + confirm, manual download/changelog links, pre-flight notes. Version-check admin notice gains an **Update now** link. Maintenance mode blocks front-end (admin + CLI still load). Procedural API: `ap_can_core_update` / `ap_run_core_update` / `ap_is_maintenance_mode`. Privacy: no domain or site identity on package download (`CoreUpdater; no-site-id` User-Agent). Tests: `tests/Admin/CoreUpdaterTest.php`, `tests/test_core_updater.py`.
- **Version checker (admin notice, no site identity):** `AP_Version_Check` GETs the public `https://agorapress.extrovertednerd.com/version.json` endpoint (filterable via `ap_version_check_url`), compares SemVer to `AP_VERSION`, and queues an admin-only “Update available” notice with **Update now** + Download + Changelog links when remote is newer. Parses optional package `sha256` for the auto-updater. Response is transient-cached (`ap_version_check`; 12h success / 1h soft failure); network/parse errors fail silently. No domain, site URL, or other identifying data is sent (GET only; generic User-Agent). Site option `version_check_enabled` (installer default on) and filter `ap_version_check_enabled` can disable checks. Wired from `admin-bootstrap.php` for users with `update_core` / `manage_options`. `AP_Admin::addNotice()` accepts optional trusted HTML (`$escape = false`) for link-bearing notices. Procedural API: `ap_version_check_enabled` / `ap_has_core_update` / `ap_get_remote_version_info` / `ap_force_version_check`. Tests: `tests/Admin/VersionCheckTest.php`, `tests/test_version_check.py`.
- **Admin screens for forums and moderation:** Full ap-admin Forums section (module-gated) — hierarchical Forums list (`forums.php` + `AP_Forums_List_Table`) with search/bulk delete, Add/Edit forum form (`forum-edit.php` + `AP_Admin_Forum_Edit`), Topics list with status views and bulk lock/sticky/approve/soft-delete (`forum-topics.php` + `AP_Forum_Topics_List_Table`), Moderation queue for pending topics/replies and reports (`forum-moderation.php` + `AP_Forum_Moderation_Queue`), Forum Groups CRUD (`forum-groups.php` + `AP_Admin_Forum_Groups`), and Settings → Forums (`options-forums.php` via `AP_Options::updateForumSettings`). Caps `manage_forums` / `moderate_forums` / `manage_options`; nonces on all state-changing actions; menu + dashboard glance links when Forum module is on. Tests: `tests/Admin/AdminForumsTest.php`, `tests/test_admin_forums.py`.
- **Forum search, flood control, anti-spam, and post approval:** Public forum search via `AP_Forum::search` / `searchTopics` / `searchPosts` (LIKE title+content, approved-only by default, pretty `/forums/search/{q}/` + plain `ap_forum_view=search&forum_s=`). Optional `check_permissions` + `user_id` filters results to forums the user can `read_forum` (front-end always on; `manage_forums` bypass). Agora `forum-search.php` + index search form + result list CSS; rewrite rules and `AP_Forum_Front` search view. `AP_Forum_Guard` enforces flood interval (`forum_flood_interval`, default 30s; mods exempt), pluggable spam (blacklist hard-reject, max-links → pending, `registerSpamChecker` + `ap_pre_forum_post_status` filter), and site-wide approval queue (`forum_posts_require_approval`). `createTopic` / `createReply` honor `check_guard` (front-end always on); pending topics/replies skip public counters; queues via `getPendingTopics` / `getPendingPosts`; approve/unapprove on `AP_Forum_Moderation`. Options seeded by installer; procedural API `ap_forum_search` / `ap_forum_is_flooding` / `ap_forum_guard_evaluate` / `ap_get_pending_topics` / `ap_approve_topic` / …. Tests: `tests/Forum/ForumSearchGuardTest.php`, `tests/test_forum_search_guard.py`.
- **Forum front-end templates:** Public forum UI is fully wired end-to-end. `AP_Rewrite` gains pretty rules for `/forums/`, `/forums/{slug}/`, `/topic/{slug}/` (plus plain query vars `ap_forum_view` / `forum_id` / `topic_id`). New `AP_Forum_Front` resolves slugs → IDs, attaches template metadata (names, locked, `can_post_topic` / `can_reply`), increments topic views, marks topics read, and tracks online presence. Create-topic and reply POSTs use nonces + per-forum ACL (`check_permissions`), then redirect with flash notices. Agora theme templates (`forum.php`, `forum-view.php`, `topic.php`) render live index/topic lists, notices, attachments, and working forms (login prompts for guests). Fallback primary nav exposes Forums when the module is on; `AP_Forum::forumsIndexUrl()` / plain vs pretty `forumUrl` / `topicUrl`. Front controller handles forum POST before render. Procedural helpers: `ap_forums_url` / `ap_forum_url` / `ap_topic_url` / `ap_is_forum_request` / `ap_handle_forum_front_post` / `ap_get_forum_notice`. Tests: `tests/Forum/ForumFrontTest.php`, `tests/test_forum_front.py`.
- **Who’s online / unread tracking:** Presence on existing `{prefix}online` (migration 0005) via `AP_Online` — track/refresh by session key, distinct online members + guest sessions, prune by window (default 15m), remove on logout, theme `getDisplay()` snapshot. Unread tracking via migration `0009_forum_online_unread.php` (`{prefix}topic_track`, `{prefix}forum_track`) + usermeta `forum_last_mark` through `AP_Forum_Read` — mark topic/forum/all read, `isTopicUnread` / `isForumUnread`, unread topic lists/counts, annotate topics with `is_unread`. Options: `forum_online_enabled`, `forum_online_window`, `forum_unread_tracking_enabled` (installer defaults on; require forum module). Procedural API: `ap_track_online` / `ap_get_online_users` / `ap_count_online` / `ap_get_online_display` / `ap_mark_topic_read` / `ap_mark_forum_read` / `ap_mark_all_forums_read` / `ap_is_topic_unread` / `ap_get_unread_topics` / … `AP_DB_VERSION` bumped to `9`. Bootstrap loads both classes. Tests: `tests/Forum/OnlineUnreadTest.php`, `tests/test_online_unread.py`.
- **Private Messaging:** Forum PM on existing `{prefix}messages` (migration 0005) via `AP_Private_Message`. Send/reply with thread roots (`parent_id`), inbox/outbox/unread lists and counts, mark read/unread, per-user soft-delete (hard purge when both sides delete), staff view with `manage_forums`, ban block on send via `AP_Forum_Moderation`, site toggle `forum_private_messaging_enabled` (installer default on; also requires forum module). Safe HTML display through `AP_Content_Format`. Procedural API: `ap_send_private_message` / `ap_reply_private_message` / `ap_get_pm_inbox` / `ap_get_pm_outbox` / `ap_count_unread_pms` / `ap_get_pm_thread` / `ap_mark_pm_read` / `ap_delete_private_message` / `ap_get_pm_folder_display` / … Bootstrap loads `class-ap-private-message.php`. Tests: `tests/Forum/PrivateMessageTest.php`, `tests/test_private_messaging.py`.
- **User groups + granular per-forum permissions:** Migration `0007_forum_permissions.php` creates `{prefix}forum_permissions` (group × forum × capability ACL; `forum_id=0` = global defaults; multi-driver). `AP_Group` manages `{prefix}groups` / `{prefix}group_members` (CRUD, membership roles member/moderator/leader, open/closed/hidden/system types) and seeds system groups `guests`, `registered`, `administrators`, `global_moderators` with virtual membership tied to login state and core caps. `AP_Forum_Permissions` resolves view/read/post/edit/attach/moderate/sticky/announce/lock/move; `manage_forums` bypasses ACL; `moderate_forums` grants moderation family; explicit group allow overrides virtual registered/guests deny (VIP-only forums); explicit group deny still wins. `AP_Forum::createTopic` / `createReply` accept `check_permissions` to enforce ACL (post_topics, post_replies, sticky/announce types). Installer seeds system groups + global defaults; editor role gains `moderate_forums`. Procedural API: `ap_create_group` / `ap_add_group_member` / `ap_user_can_forum` / `ap_set_forum_permission` / … `AP_DB_VERSION` bumped to `7`. Tests: `tests/Forum/ForumGroupsPermissionsTest.php`, `tests/test_forum_groups_permissions.py`.
- **Forum attachments with quotas:** Migration `0006_forum_attachments.php` creates `{prefix}forum_attachments` (multi-driver) linking media library files to forum posts/topics. `AP_Forum_Attachment` uploads via `AP_Media`, enforces enable toggle, per-file max size, allow-listed extensions, max-per-post, and per-user storage quota (options seeded by installer). Orphan upload + `assignToPost` for post-after-upload flows; download counter; cascade delete on post/topic force-delete. Display rows expose `attachments` on topic posts. Procedural API: `ap_handle_forum_attachment_upload` / `ap_get_forum_attachments` / `ap_delete_forum_attachment` / `ap_can_upload_forum_attachment` / … `AP_DB_VERSION` bumped to `6`. Tests: `tests/Forum/ForumAttachmentTest.php`, `tests/test_forum_attachments.py`.
- **BBCode + Markdown + limited safe HTML:** Pure-PHP `AP_Content_Format` formats user content for display without external deps. Modes: `auto` (default: BBCode → Markdown → whitelist kses), `bbcode`, `markdown`, `html`, `plain`. BBCode: b/i/u/s, url, email, img, quote, code, list, color, size, center, spoiler. Markdown subset: headings, bold/italic/strike, links/images, lists, blockquotes, fenced/inline code, hr. Kses allow-list strips scripts/event handlers and blocks `javascript:`/`data:` URLs. Procedural API: `ap_format_content` / `ap_bbcode_to_html` / `ap_markdown_to_html` / `ap_kses` / `ap_allowed_html` / `ap_is_safe_url`. Filters: `ap_format_content`, `ap_allowed_html`. Forum posts store rendered HTML in `post_content_filtered` on create/update; display rows expose `content_html` for the Agora topic template. Bootstrap loads `class-ap-content-format.php`. Tests: `tests/Content/ContentFormatTest.php`, `tests/test_content_format.py`.
- **Forum hierarchy, topics, posts/replies:** `AP_Forum` domain API on dedicated tables — hierarchical forums/categories (`insertForum` / `updateForum` / `deleteForum`, cycle-safe parents, unique sibling slugs, `getHierarchy` / `getIndexData`), topics with sticky/announce/global ordering (`createTopic` with first post, `updateTopic`, soft/force `deleteTopic`, view counter), and posts/replies (`createReply`, `updatePost`, `deletePost`, chronological `getPosts`). Denormalized `topic_count` / `post_count` / last-post pointers stay in sync; closed forums and locked topics block posting (bypass via `check_open`). Procedural helpers (`ap_insert_forum`, `ap_create_topic`, `ap_create_forum_reply`, …); Agora theme data providers load live rows. Bootstrap loads `class-ap-forum.php`. Tests: `tests/Forum/ForumModelTest.php`, `tests/test_forum_model.py`.
- **Dedicated forum tables (schema v5):** Migration `0005_forum_tables.php` creates `{prefix}forums`, `{prefix}topics`, `{prefix}forum_posts`, `{prefix}groups`, `{prefix}group_members`, `{prefix}messages`, `{prefix}ranks`, `{prefix}reports`, and `{prefix}online` with multi-driver DDL (MySQL/MariaDB InnoDB utf8mb4, SQLite, PostgreSQL). Forums are hierarchical (`parent_id`, type category/forum/link, status open/closed/hidden, denormalized topic/post counts + last post pointers). Topics support sticky/announce/global types, open/locked/moved/deleted status, approval, views, and reply counts. `forum_posts` are separate from blog comments (poster, content, IP, edit trail, approval, report flag). Groups + members, private messages, ranks, moderation reports, and who’s-online sessions complete the SPEC §4 forum set while sharing core users/caps/media. `AP_Forum::baseTables()` / `tables()` helpers; `AP_DB_VERSION` bumped to `5`. Tests: `tests/Database/ForumTablesMigrationTest.php`, `tests/test_forum_tables.py`, extended migrator suite.
- **Theme uploader (classic WP themes):** `AP_Theme_Installer` installs classic WordPress / AgoraPress themes from `.zip` packages into `ap-content/themes/{slug}/`. Validates zip safety (path traversal, zip-bomb soft limits, blocked script extensions), requires `style.css` with a Theme Name header, requires `index.php` for parent themes, rejects block/FSE packages by default (`theme.json` or HTML under `templates/`), supports folder-root and zip-root layouts, optional overwrite, and delete (never the active or protected Agora theme). Admin Appearance → Themes (`ap-admin/themes.php`, caps `switch_themes` / `install_themes` / `delete_themes`): grid of installed themes, Activate/Delete with nonces, multipart zip upload + overwrite checkbox, notice about the Classic WP Compatibility Layer. Procedural helpers: `ap_install_theme_from_zip` / `ap_upload_theme` / `ap_delete_theme`. Bootstrap loads `class-ap-theme-installer.php`. Tests: `tests/Theme/ThemeInstallerTest.php`, `tests/test_theme_uploader.py`.
- **Classic WordPress Theme Compatibility Layer:** `ap-includes/compatibility/` provides WP-style template tags and function shims (`have_posts` / `the_title` / `get_header` / `wp_enqueue_*` / `add_action` / …), hook name mapping (`wp_enqueue_scripts` → `ap_enqueue_scripts`, `after_setup_theme` → `ap_after_setup_theme`, …), per-theme mode (`auto`/`on`/`off` via option `ap_theme_compat_modes`), block/FSE detection (`theme.json` / HTML templates — out of scope), and safe `functions.php` loading from `AP_Theme::setup()`. Conversion helper `AP_Theme_Converter` + CLI `php ap-includes/compatibility/cli-convert.php <theme-path>` (`--json`) reports classic vs block, shimmed/unshimmed symbols, score, and conversion tips (dry-run only). Bootstrap loads `compatibility/load.php`; shims load lazily for classic non-Agora themes. Procedural helpers: `ap_load_theme_compat` / `ap_is_block_theme` / `ap_analyze_wp_theme` / `ap_theme_compat_*`. Tests: `tests/Theme/ThemeCompatTest.php`, `tests/test_theme_compat.py`.
- **Widget / modular area system:** `AP_Widgets` registers theme sidebars (regions) and widget types; placements in option `sidebars_widgets`, multi-instance settings in `widget_{id_base}`. Built-ins: Text, Recent Posts, Categories, Search, Pages, Navigation Menu. Procedural API: `ap_register_sidebar(s)` / `ap_register_widget` / `ap_is_active_sidebar` / `ap_dynamic_sidebar` / `ap_get_sidebars`. Admin Appearance → Widgets (`widgets.php`, cap `edit_theme_options`) to add, configure, reorder, move, and remove instances. Agora theme registers `sidebar-1` + `footer-1`, ships `sidebar.php`, two-column layout when the primary area is active, footer widget row. Installer seeds empty `sidebars_widgets`. Tests: `tests/Widget/WidgetsTest.php`, `tests/test_widgets.py`.
- **Full action/filter hook system:** WordPress-inspired registry in `AP_Hook` / `AP_Hooks` (`class-ap-hook.php`, `class-ap-hooks.php`) with procedural API in `hooks.php`. Actions: `ap_add_action` / `ap_do_action` / `ap_do_action_ref_array` / `ap_remove_action` / `ap_remove_all_actions` / `ap_has_action` / `ap_did_action` / `ap_current_action` / `ap_doing_action`. Filters: `ap_add_filter` / `ap_apply_filters` / `ap_apply_filters_ref_array` / `ap_remove_filter` / `ap_remove_all_filters` / `ap_has_filter` / `ap_current_filter` / `ap_doing_filter`. Features: priority ordering (lower first), stable same-priority order, accepted_args, callback dedupe, remove-all by priority, nested hook stack, mid-run add at same/later priority, mid-run remove, catch-all `"all"` hook (fires first; `current_filter` reports the target hook), `ap_reset_hooks` for tests. Powers plugins, themes, and core lifecycle hooks (`ap_loaded`, `ap_plugins_loaded`, `ap_enqueue_scripts`, …). Tests: `tests/Hooks/HooksTest.php`, `tests/test_hooks.py`.
- **Plugin discovery, activation/deactivation, headers:** `AP_Plugin` scans `ap-content/plugins/` for single-file and one-level folder plugins with classic `Plugin Name:` headers (WordPress-compatible fields including Version, Author, Requires PHP, License, Text Domain). Active basenames live in option `active_plugins` (JSON list). Activate includes the main file, runs `ap_register_activation_hook` callbacks and `ap_activate_plugin` / per-plugin actions; deactivate runs deactivation hooks then removes from the list. Bootstrap loads active plugins once and fires `ap_plugins_loaded` before `ap_loaded`. Procedural API: `ap_get_plugins` / `ap_get_plugin_data` / `ap_activate_plugin` / `ap_deactivate_plugin` / `ap_is_plugin_active` / `ap_plugin_basename` / `ap_register_activation_hook` / `ap_register_deactivation_hook`. Admin screen `ap-admin/plugins.php` (cap `activate_plugins`, nonce-protected activate/deactivate links, menu “Installed Plugins”). Installer seeds empty `active_plugins`. Tests: `tests/Plugin/PluginApiTest.php`, `tests/test_plugins.py`.
- **Native theme API complete (style.css, child themes, enqueue):** `AP_Theme` gains child-theme helpers (`isChildTheme`, `getStyleCssUri`, `getDeclaredParent`, `isValidTheme`, screenshot path/URI), validates `Template` parent on discovery/activation, and exposes `Is Child` / `Parent` / `Screenshot` on `listThemes()`. New `AP_Assets` implements WordPress-inspired register → enqueue → print for styles and scripts (dependency order, versions, media, footer scripts, inline CSS/JS). Procedural API: `ap_enqueue_style` / `ap_enqueue_script` / `ap_register_*` / `ap_print_*` / `ap_head` / `ap_footer` / `ap_is_child_theme` / `ap_get_style_css_uri`. Minimal hook foundation in `hooks.php` (`ap_add_action` / `ap_do_action` / `ap_add_filter` / `ap_apply_filters` + remove/has/did) powers `ap_enqueue_scripts` / `ap_head` / `ap_footer` / `ap_after_setup_theme`. Default Agora theme enqueues `style.css` via `ap_enqueue_scripts` (parent then child when a child of Agora is active); header/footer call `ap_head()` / `ap_footer()`. Tests: `tests/Theme/ThemeApiTest.php`, extended theme loader/agora tests + pytest.
- **Hall of Fame (voluntary domain registration only — no telemetry):** Chose Hall of Fame over anonymous installer pings. Core `AP_Hall_Of_Fame` never runs during install or ordinary requests; join/leave are explicit `manage_options` actions with nonces. Registration payload is domain + action only (withdrawal token on leave) — no email, user id, or environment fingerprint. Local options track membership; dashboard shows an opt-in prompt (dismissible without contacting the project site); Settings → Hall of Fame for join/leave + unobtrusive donation-footer toggle (default on). Installer seeds empty membership and does not phone home. Procedural helpers: `ap_hall_of_fame_is_joined` / `ap_hall_of_fame_status` / `ap_show_donation_button` / `ap_hall_of_fame_domain`. Tests: `tests/Admin/HallOfFameTest.php`, `tests/test_hall_of_fame.py`.
- **Nonces on all forms (CSRF):** Core `AP_Nonce` (HMAC-SHA256, 12h ticks + previous-tick grace) already gated every state-changing admin POST (posts/pages, terms, comments, media, users/profile, settings groups, nav menus, theme options, quick draft, login/register/lost-password/reset). Extended API: `ap_nonce_url` / `AP_Nonce::url` for GET action links, `ap_verify_request_nonce` / `ap_check_request_nonce` (accepts `_ap_nonce` and `_wpnonce` alias). Admin **Log out** is nonce-protected (`log-out` action; header uses `ap_nonce_url`). Installer keeps session `ap_csrf` tokens (no salts before `ap-config.php`). Form-audit + unit tests: `tests/Security/NonceTest.php`, `tests/test_nonces.py`.
- **Dashboard home with stats:** Admin `index.php` is a real At a Glance / Activity / Quick Draft home. `AP_Admin_Dashboard` aggregates module-aware counts (published/draft/pending posts & pages, approved+pending comments, users), recent published content, and recent comments; forum slot is reserved (“Coming soon” when Forum is enabled until Phase 5 tables exist). Quick Draft saves a draft post with nonce + `edit_posts` gates when Blog is on. Cards and layout use responsive CSS (`ap-dashboard-grid`, glance list, activity list). Tests: `tests/Admin/AdminDashboardTest.php`, `tests/test_admin_dashboard.py`.
- **Settings API + core settings screens:** WordPress-inspired Settings API (`AP_Settings` + `ap_register_setting` / `ap_add_settings_section` / `ap_add_settings_field` / `ap_settings_fields` / `ap_do_settings_sections`) with option-group registration, sanitize callbacks, section/field rendering, nonce-backed group save, and error queue. Core groups: general, modules, writing, reading, discussion, media, permalink (`registerCore()` on bootstrap). Admin screens: `options-general.php` (title, tagline, URLs, admin email, membership, default role, timezone/date/time), `options-modules.php` (three independent Static Pages / Blog / Forum toggles; at least one required), `options-writing.php`, existing `options-reading.php` (Settings API nonces), `options-discussion.php` (comments + avatars), `options-media.php` (image sizes + year/month folders), `options-permalink.php` (structure presets + category/tag bases). Helpers on `AP_Options` (`isModuleEnabled` / `updateModules` / `updateGeneralSettings` / …); `AP_Media::useYearMonthFolders()` honors `uploads_use_yearmonth_folders`; admin menu lists all settings screens and hides Blog/Pages items when modules are off. Installer seeds discussion/media/writing defaults. Tests: `tests/Options/SettingsApiTest.php`, `tests/test_settings_api.py`.
- **Capability checks on all admin screens:** Every privileged `ap-admin` entry point calls `AP_Admin::requireCapability()` after login (`read` shell gate remains). Screen map: Posts/Pages list+new (`edit_posts`/`edit_pages`), post/revision edit (`edit_post`/`edit_page` meta), Comments (`moderate_comments`), Categories/Tags (`manage_categories`), Media (`upload_files`), Users (`list_users`/`create_users`/`edit_users`), Theme Options + Menus (`edit_theme_options`), Reading (`manage_options`), Profile/Dashboard (`read`). Handlers enforce the same caps (and meta-caps for own vs others): post save/row/bulk, media upload/save/delete, comment moderation, term CRUD. Contributors without `publish_*` are forced to `pending` instead of published. Helpers: `AP_Admin::userCan`, `editCapabilityForPostType`, `editMetaCapForPostType`, `deleteMetaCapForPostType`, `publishCapabilityForPostType`, `screenCapabilities`. Tests: `tests/Admin/AdminCapabilityTest.php`, `tests/test_admin_capabilities.py`.
- **Avatars (local upload + Gravatar):** `AP_Avatar` resolves custom uploads (usermeta `ap_avatar` → attachment) then Gravatar by email, with site options `show_avatars` / `avatar_default` / `avatar_rating` (seeded by installer). Local uploads via `AP_Media` (JPG/PNG/GIF/WebP, 2 MiB cap); remove + replace; user delete cleans avatar files. Procedural API: `ap_get_avatar` / `ap_get_avatar_url` / `ap_get_avatar_data` / `ap_upload_user_avatar` / `ap_delete_user_avatar` / `ap_show_avatars`. Template tags `ap_the_author_avatar` / `ap_get_the_author_avatar`. Admin profile & Edit User: multipart form, avatar preview, file input, remove checkbox; users list shows 32px avatars. Offline mystery-person SVG data URI when default is mystery and no email. Tests: `tests/User/AvatarTest.php`, `tests/test_avatars.py`.
- **Registration, email verification, password reset:** Public self-registration gated by option `users_can_register` (installer default off); optional email verification via `require_email_verification` (default on) creates accounts with `user_status=1` until confirmed. HMAC one-time keys in `user_activation_key` (purpose:timestamp:hmac, 24h TTL) for verify + reset links; password-reset emails never leak account existence; 60s per-account reset cooldown; successful reset revokes sessions via `AP_User::updatePassword`. Core: `AP_Registration`, `AP_Mail` (test outbox), procedural helpers `ap_register_user` / `ap_verify_user_email` / `ap_request_password_reset` / `ap_check_password_reset_key` / `ap_reset_password` / `ap_mail` / `ap_users_can_register` / `ap_require_email_verification`. UI: `ap-admin/login.php` actions `register`, `lostpassword`, `rp`, `verifyemail` with nonces; pending-user login message. Tests: `tests/User/RegistrationTest.php`, `tests/test_registration.py`.
- **User list / edit / profile admin screens:** All Users list (`ap-admin/users.php`) with role views, search, pagination, bulk role change + delete, and row delete; Add New User (`user-new.php`); Edit User (`user-edit.php`); Profile for the current user (`profile.php`). Shared form logic in `AP_Admin_User_Edit` (nonce + capability gates, password confirm, role only when `promote_users`). Core user CRUD + usermeta on `AP_User` (`create` / `update` / `delete` / `query` / `count` / `countByRole` / `getMeta` / `updateMeta` / profile fields first_name, last_name, nickname, description; last-admin protection). Sidebar menu items Users (`list_users`) and Profile (`read`); topbar name links to profile. Procedural helpers: `ap_create_user`, `ap_update_user`, `ap_delete_user`, `ap_get_users`, `ap_count_users`, `ap_get_user_meta`, `ap_update_user_meta`, `ap_generate_password`. Avatar upload/remove lives on profile & edit (see Avatars entry). Tests: `tests/Admin/AdminUsersTest.php`, `tests/test_admin_users.py`.
- **Full roles & capabilities system** (`AP_Roles` + `ap_*` helpers): WordPress-inspired role map stored in option `ap_user_roles` with five built-in roles (administrator, editor, author, contributor, subscriber); per-user assignment in usermeta `ap_capabilities` (+ `ap_user_level`); role CRUD (`addRole` / `removeRole` / `addCap` / `removeCap`); user assignment (`setUserRole` / `addUserRole` / `removeUserRole` / direct user caps); `userCan` / `currentUserCan` with meta-cap mapping for `edit_post` / `delete_post` / `read_post` / page variants / `edit_comment` (own vs others, private/published); primitive caps for content, users, media, comments, themes/plugins foundation, and forum stubs. Installer seeds roles + `default_role=subscriber` and assigns administrator via the API. Bootstrap loads roles and calls `ensureDefaults`. Admin shell requires `read`, exposes `requireCapability` / `denyAccess`, and filters sidebar menu items by capability. Procedural API: `ap_user_can`, `ap_current_user_can`, `ap_get_roles`, `ap_set_user_role`, `ap_map_meta_cap`, etc. Tests: `tests/User/RolesTest.php`, `tests/test_roles.py`.
- **Template tags, Menus, RSS/Atom, Front-page (Reading) settings:** Front-end template tags in `ap-includes/template-tags.php` (`ap_the_title` / `ap_the_content` / `ap_the_excerpt` / `ap_the_permalink` / `ap_the_date` / `ap_the_author` / `ap_bloginfo` / `ap_body_class`, with get_* variants). Navigation menus (`AP_Nav_Menu`): theme locations, option-backed menus/items (custom URL, page, post, category, tag), assign locations, escaped HTML render; admin screen `ap-admin/nav-menus.php`; procedural helpers `ap_register_nav_menus` / `ap_nav_menu` / `ap_has_nav_menu`. Syndication feeds (`AP_Feed`): RSS 2.0 + Atom at pretty `/feed/` or `?feed=rss2|atom`, item count and full text vs summary from Reading settings; short-circuit in front controller before the theme. Reading settings (`AP_Options` helpers + `ap-admin/options-reading.php`): `show_on_front` / `page_on_front` / `page_for_posts` / `posts_per_page` / `posts_per_rss` / `rss_use_excerpt`; `AP_Rewrite::applyReadingSettings()` maps home → static page or latest posts and posts-page → blog index; Agora theme uses `front-page.php`, primary/footer menus, and RSS alternate links. Installer seeds Reading + empty menu options. Tests: `tests/Template/TemplateTagsTest.php`, `tests/Menu/NavMenuTest.php`, `tests/Feed/FeedTest.php`, `tests/Options/ReadingSettingsTest.php`, `tests/test_template_tags_menus_rss.py`.
- Default theme **Agora** polish: lightweight image-free pure CSS with **exactly six color schemes** (3 light + 3 dark) — Marble, Parchment, Cloud, Obsidian, Midnight, Charcoal — selectable via Theme Options (`ap-admin/theme-options.php`) and option `agora_color_scheme` (default `marble`). Scheme API in `ap-content/themes/agora/functions.php` (`agora_get_color_schemes`, `agora_get/set_color_scheme`, `agora_body_class`); body classes `agora-scheme-{slug}` + `agora-mode-light|dark`; accessible typography, sticky header, cards, focus rings, print + reduced-motion styles. Installer seeds `agora_color_scheme`. Admin menu item “Theme Options”. Tests: `tests/Theme/AgoraThemeTest.php`, `tests/test_agora_theme.py`.
- Front-end template loader + classic hierarchy (`AP_Theme` + `ap_*` helpers): active theme via `stylesheet` / `template` options (default `agora`), style.css header parsing and theme discovery, child→parent template location, WordPress-inspired hierarchy from `AP_Query` conditionals (home, single, page + custom page templates, category/tag/tax, author, date, search, 404, archives), `get_header` / `get_footer` / `get_sidebar` partials, `functions.php` load (parent then child), and `render()` / `ap_template_loader()` wired from front controller `index.php`. Ships a functional default theme under `ap-content/themes/agora/` (index, single, page, home, archive, search, 404, category/tag/author, example Full Width page template; color-scheme polish listed separately above). Installer seeds `stylesheet`/`template`. Tests: `tests/Theme/ThemeLoaderTest.php`, `tests/test_theme_loader.py`.
- Permalinks / rewrite rules (`AP_Rewrite` + `ap_*` helpers): pretty URL structures (Plain, Day and name, Month and name, Numeric, Post name, custom tags `%year%` `%monthnum%` `%day%` `%post_id%` `%postname%` `%category%` `%author%`), optional `category_base` / `tag_base`, rule generation + flush into options (`rewrite_rules`), `parseRequest` / `parseFromGlobals` path → query vars for `AP_Query`, link builders for posts/pages/terms/authors/search/home/site, Apache/Nginx front-controller snippets. Front controller `index.php` parses the request and sets the main query. Installer seeds `permalink_structure`, `category_base`, `tag_base`, `rewrite_rules`. Admin post edit shows the computed permalink. Root `.htaccess` and `docker/nginx.conf.example` document pretty-permalink routing. Tests: `tests/Rewrite/RewriteTest.php`, `tests/test_rewrite.py`.
- Comments + moderation (`AP_Comment` + admin): migration `0004_core_comments_commentmeta.php` creates `{prefix}comments` and `{prefix}commentmeta` (MySQL/SQLite/PostgreSQL). Nested threads via `comment_parent`; statuses `1` (approved) / `0` (pending) / `spam` / `trash`; approve/unapprove/spam/unspam/trash/untrash/delete with `posts.comment_count` sync; pluggable spam checkers (`registerSpamChecker` + future `ap_pre_comment_approved` filter); commentmeta CRUD; query/list, status counts, and nested `getTree()`. Procedural helpers: `ap_insert_comment`, `ap_approve_comment`, `ap_get_comment_tree`, `ap_register_comment_spam_checker`, etc. Admin: `edit-comments.php` list with All/Pending/Approved/Spam/Trash views, search, bulk + row moderation actions, Comments menu item. `AP_DB_VERSION` bumped to `4`. Tests: `tests/Comment/CommentModelTest.php`, `tests/Database/CommentsCommentmetaMigrationTest.php`, `tests/Admin/AdminCommentsTest.php`, `tests/test_comments.py`.
- Taxonomies (`AP_Taxonomy` + admin): migration `0003_core_terms_taxonomies.php` creates `{prefix}terms`, `{prefix}term_taxonomy`, `{prefix}term_relationships` (MySQL/SQLite/PostgreSQL). Built-in `category` (hierarchical) and `post_tag` (flat); registerable custom taxonomies; term CRUD with unique slugs, hierarchy + cycle prevention, default Uncategorized category; object relationships (`setObjectTerms` / `getObjectTerms`) with auto-create for tag names and count maintenance. `AP_Query` supports `cat`, `category_name`, `category__in`/`__not_in`/`__and`, `tag` / `tag_id` / `tag__*`, `tax_query` (EXISTS subqueries), and `is_category` / `is_tag` / `is_tax` flags. Procedural helpers: `ap_register_taxonomy`, `ap_insert_term`, `ap_set_post_categories`, `ap_set_post_tags`, etc. Admin: `edit-tags.php` list/add/edit/delete for Categories & Tags, menu items, post edit sidebar checklist + tags input, list table Categories/Tags columns and `?cat=` category filter. `AP_DB_VERSION` bumped to `3`. Tests: `tests/Taxonomy/TaxonomyTest.php`, `tests/Database/TermsTaxonomyMigrationTest.php`, `tests/test_taxonomies.py`, extended `AdminPostsTest`.
- Media Library (`AP_Media` + admin): secure multi-file uploads into `ap-content/uploads/` (optional year/month folders), allow-listed MIME/extension checks with `finfo` content sniffing and executable rejection, attachment posts (`post_type=attachment`, status `inherit`) with meta for relative path, filesize/dimensions, and image alt text; query/list by search, MIME group, and date; permanent delete removes DB row + file. Upload dir hardening writes `.htaccess` (static media allowed, script extensions denied) + silent `index.php` on first use. Procedural helpers: `ap_handle_upload`, `ap_insert_attachment`, `ap_delete_attachment`, `ap_get_attachment_url`, `ap_upload_dir`, etc. Admin: `upload.php` (grid/list toggle, multi-file + drag-and-drop upload, bulk delete, MIME/date/search filters), `media.php` attachment details (title, caption, alt, description, file URL), Media menu item. Tests: `tests/Media/MediaLibraryTest.php`, `tests/test_media_library.py`.
- Post revisions + autosave (`AP_Post` + admin): revision rows (`post_type=revision`, `post_status=inherit`, `post_parent` = parent ID) snapshot title/content/excerpt on update when the type supports `revisions`; max kept via `AP_POST_REVISIONS` / `DEFAULT_MAX_REVISIONS` with `pruneRevisions()`; one autosave per author (`{id}-autosave-{userId}`) that does not mutate the parent; restore / delete / list / count APIs; force-delete parent removes child revisions. Procedural helpers: `ap_save_post_revision`, `ap_autosave_post`, `ap_get_post_revisions`, `ap_restore_post_revision`, `ap_get_post_autosave`, etc. Admin: Autosave button on edit form, Revisions metabox, history screen `ap-admin/revision.php` with restore/delete. Tests: extended `PostModelTest` / `AdminPostsTest`, pytest smokes.
- Admin list tables + edit screens for Posts and Pages (`ap-admin/`): login (`login.php`), dashboard (`index.php`), list table (`edit.php?post_type=post|page`) with status views (All/Published/Draft/Pending/Private/Scheduled/Trash), search, pagination, bulk trash/untrash/delete/status change, row actions; create/edit screens (`post-new.php`, `post.php`) with title, slug, content, excerpt, status/visibility (public/password/private), publish date, sticky (posts), page attributes (parent, order, template), discussion checkbox; nonces on all state-changing forms (`AP_Nonce` + `ap_create_nonce` / `ap_check_nonce` / `ap_nonce_field`); escaping helpers (`ap_esc_html`, `ap_esc_attr`, `ap_sanitize_text_field`); lightweight accessible admin CSS (light/dark via `prefers-color-scheme`). Capability checks: see “Capability checks on all admin screens” above. Tests: `tests/Admin/AdminPostsTest.php`, `tests/test_admin_posts.py`.
- Post statuses, types, hierarchical pages (`AP_Post` + `ap_*` helpers): built-in statuses (publish, draft, pending, private, future, trash, auto-draft, inherit) and types (post, page, revision, attachment); lightweight custom status/type registration; insert/update/trash/untrash/delete with unique slugs, future auto-schedule, password protection, sticky + page-template meta; hierarchical pages (`post_parent`, ancestors, page path, tree, cycle prevention, re-parent on force-delete); simple list query and postmeta helpers. Bootstrap loads `class-ap-post.php` and registers builtins. Tests: `tests/Post/PostModelTest.php`, `tests/test_post_model.py`.
- Core tables migration `0002_core_posts_postmeta.php`: creates `{prefix}posts` and `{prefix}postmeta` with multi-driver DDL (MySQL/MariaDB InnoDB utf8mb4, SQLite, PostgreSQL). Posts hold author, dates (local + GMT), content/title/excerpt, status, comment/ping flags, password, slug (`post_name`), hierarchy (`post_parent`), GUID, menu order, type (post/page/revision/attachment/CPT), mime type, and comment count — indexes on name, type+status+date, parent, and author. Postmeta is key/value by `post_id`. `AP_DB_VERSION` bumped to `2`. Tests: `tests/Database/PostsPostmetaMigrationTest.php`, extended Migrator suite + pytest.
- “Already installed” protection: `AP_Installer::configExists()` / `alreadyInstalledMessage()`; `writeConfigFile()` and `run()` refuse to overwrite an existing config; web installer (`install/index.php`) returns HTTP 403 with an “Already installed” screen when `ap-config.php` is present (post-install success screen only when the same session just finished install — no `?step=done` loophole); CLI exits early with code 3 before requirements/DB work. Tests: `InstallerTest`, `InstallUiTest`, `CliInstallTest`, `tests/test_installer.py`.
- CLI install path (`install/cli.php` + `AP_Cli_Install`): non-interactive install sharing `AP_Installer` / `AP_Requirements` with the web flow — flags for DB driver (mysql/sqlite/pgsql), credentials, table prefix, site title/URL, admin account; env fallbacks `AP_ADMIN_PASSWORD` / `AP_DB_PASSWORD`; requirements check (optional `--skip-requirements`); refuses existing `ap-config.php`; exit codes 0/1/2/3. Documented under README Option C. Tests: `tests/Install/CliInstallTest.php`, `tests/test_cli_install.py`.
- Session handling (`AP_Session` + `ap_*` helpers): signed HMAC-SHA256 auth cookies (`user_id|expiration|token|hmac`) using `AP_LOGGED_IN_KEY` + `AP_LOGGED_IN_SALT`, HttpOnly / SameSite=Lax / Secure-when-HTTPS, default 2-day lifetime (14 days with “remember me”), per-session tokens stored hashed in usermeta (`session_tokens`) for server-side revoke, `login` / `logout` / `setAuthCookie` / `clearAuthCookie` / `getCurrentUser` / `isLoggedIn`. Password changes via `AP_User::updatePassword()` destroy all session tokens and bind cookies to a password-hash fragment so outstanding cookies fail validation. Procedural API: `ap_login`, `ap_logout`, `ap_set_auth_cookie`, `ap_clear_auth_cookie`, `ap_is_user_logged_in`, `ap_get_current_user`, `ap_get_current_user_id`, `ap_auth_cookie_name`, `ap_destroy_user_sessions`. Bootstrap loads the session layer after users. Tests: `tests/User/SessionTest.php`, `tests/test_session.py`.
- Basic authentication (`AP_User` + `ap_*` helpers): Argon2id password hashing when `PASSWORD_ARGON2ID` is available (fallback `PASSWORD_DEFAULT`), `password_verify` checks, transparent rehash on successful login, user lookup by id/login/email/nicename, and `authenticate(login|email, password)` against `{prefix}users` (rejects inactive `user_status !== 0`; dummy-hash path for unknown accounts). Procedural API: `ap_hash_password`, `ap_check_password`, `ap_password_needs_rehash`, `ap_authenticate`, `ap_get_user_by`. Installer seed hashing delegates to `AP_User`. Bootstrap loads the user class after the DB layer. Tests: `tests/User/UserAuthTest.php`, `tests/test_user_auth.py`.
- Web installer (`install/index.php`): multi-step flow **requirements → database → site info + admin → tables + config**. `AP_Requirements` checks PHP 8.2+, required extensions (PDO driver, mbstring, json, curl, fileinfo, zip, gd|imagick), recommended intl, and writable site root / `ap-content` / uploads. `AP_Installer` generates unique auth salts, writes `ap-config.php` (never commit), runs versioned migrations, seeds core options (including module toggles) and the first administrator with Argon2id password hashing. Supports mysql/sqlite/pgsql; refuses to overwrite an existing config. Bootstrap “not installed” page links to `/install/`. Tests: `tests/Install/`, `tests/test_installer.py`.
- Core tables migration `0001_core_options_users.php`: creates `{prefix}options`, `{prefix}users`, and `{prefix}usermeta` with multi-driver DDL (MySQL/MariaDB InnoDB utf8mb4, SQLite, PostgreSQL). Options include `option_name` / `option_value` / `autoload`; users hold login, Argon2id-ready `user_pass`, email, nicename, status, display name; usermeta is key/value by `user_id`. `AP_DB_VERSION` bumped to `1`. Tests: `tests/Database/CoreTablesMigrationTest.php`, extended Migrator suite + pytest.
- Versioned schema / migration system (`ap-includes/class-ap-migrator.php`, `AP_Migration`, `schema/migrations/`): discovers numbered `NNNN_slug.php` migrations, tracks applied versions in `{prefix}schema_migrations`, applies pending upgrades in order (`migrate()` / `pending()` / `needsMigration()`), honors table prefix and multi-driver registry DDL (mysql/sqlite/pgsql). Bootstrap loads the migrator without auto-running migrations. Tests: `tests/Database/MigratorTest.php`, `tests/test_migrator.py`
- Table prefix support: configurable `$table_prefix` (default `ap_`) normalized to a safe SQL fragment, exposed as `AP_TABLE_PREFIX` / `ap_get_table_prefix()`; Docker `AP_TABLE_PREFIX` env honored when config omits `$table_prefix`. Core + forum base table lists (`ap_core_base_tables`, `ap_forum_base_tables`, `ap_all_base_tables`) and helpers `ap_prefixed_table()` / `ap_prefixed_tables()`. `AP_DB` normalizes prefixes, applies them in `table()` / insert/update/delete, exposes WP-style `$apdb->options`/`$apdb->users`/… properties, `setPrefix()`, `tables()`, `resolveConfiguredPrefix()`. Tests: `tests/Database/TablePrefixTest.php`, extended Config/Database suites + pytest.
- Database abstraction (`ap-includes/class-ap-db.php`): `AP_DB` / global `$apdb` via lazy `ap_db()`, PDO only with real prepared statements (`ATTR_EMULATE_PREPARES` off), drivers mysql/sqlite/pgsql (DSN builder, host:port), prefixed `table()`, `query`/`getVar`/`getRow`/`getCol`/`getResults`, `insert`/`update`/`delete` with bound values, transactions, `AP_DB_Exception`. Bootstrap loads the class without connecting. Tests: `tests/Database/DatabaseTest.php`, `tests/test_database.py`
- Config loading (`ap-includes/load-config.php`): load/validate `ap-config.php`, required constants, defaults (charset, debug, telemetry off), table-prefix normalize + `AP_TABLE_PREFIX` / `ap_get_table_prefix()`, content path constants, debug error settings; incomplete/invalid config returns friendly 503 HTML. Wired from bootstrap via `ap_load_config()`. Tests: `tests/Config/ConfigLoadTest.php`, `tests/test_config_load.py`
- Initial project structure and guiding documents
- Repository layout matching SPEC.md §2: `index.php`, `ap-config-sample.php`, `.htaccess`, nginx example, `composer.json`, `docker-compose.yml`, `LICENSE` (GPLv2-or-later), `ap-admin/`, `ap-includes/` (core class stubs + `compatibility/`), `ap-content/` (themes, plugins, mu-plugins, languages, uploads), `tests/`
- Structure verification: `php tests/Structure/assert-structure.php`, `tests/test_repository_structure.py`, `tests/Structure/RepositoryStructureTest.php`
- Coding standards: PSR-12 adapted via `phpcs.xml.dist`, `CODING_STANDARDS.md`, PHPCS dev dependency, and Composer scripts `cs` / `cs:check` / `cs:fix`
- Docker Compose local stack: `docker-compose.yml` + `docker/Dockerfile` (PHP 8.3 Apache, SPEC extensions, mod_rewrite), MySQL 8 utf8mb4 with healthchecks; contract tests under `tests/Docker/` and `tests/test_docker_compose.py`
- Front controller + bootstrap: `index.php` loads `ap-includes/bootstrap.php` / `version.php`; missing `ap-config.php` (or PHP below 8.2) returns a friendly HTTP 503 HTML page without fatals; installed path loads config + core includes. Tests: `tests/Bootstrap/`, `tests/test_bootstrap.py`
- Sample site config `ap-config-sample.php`: DB driver (mysql default; sqlite/pgsql documented), utf8mb4 + `utf8mb4_unicode_ci`, `$table_prefix = 'ap_'`, auth keys/salts, debug flags off, `AP_TELEMETRY` false, guarded `AP_ABSPATH`, Docker Compose credential notes. Tests: `tests/Config/`, `tests/test_config_sample.py`
- README.md: vision summary (core principles, three module toggles, non-goals), requirements, Docker Compose + manual quick-start, layout, development commands, license/links. Tests: `tests/Readme/`, `tests/test_readme.py`
- Basic PHPUnit / static analysis skeleton: `phpunit.xml.dist` (bootstrap `tests/bootstrap.php`, suite under `tests/`, source `ap-includes/`), Composer scripts `test` / `cs` / `cs:check` / `cs:fix`, PHPCS via `phpcs.xml.dist`, GitHub Actions CI (PHP 8.2–8.4 hard-fail). Skeleton contract tests: `tests/Phpunit/`, `tests/test_phpunit_skeleton.py`
- This changelog (`CHANGELOG.md`) following Keep a Changelog + SemVer. Contract tests: `tests/Changelog/`, `tests/test_changelog.py`
